<%=
  if @cluster_program.is_a? Program
    @program = @cluster_program
    render 'programs/sidebar' do
      render 'show'
    end
  else
    @cluster = @cluster_program
    render 'clusters/cluster_header' do
      render 'show'
    end
  end
%>