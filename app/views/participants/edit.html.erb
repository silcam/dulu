<%= render layout: 'programs/sidebar' do %>
    <h3><%= @participant.person.full_name %></h3>

    <%= form_for @participant do |form| %>
        <p>
          <%= form.label :program_role_id, t(:Role) %>
          <%= form.collection_select :program_role_id, ProgramRole.all, :id, :t_name %>
        </p>

        <p>
          <%= form.label :start_date, t(:Start_date) %>
          <%= render 'layouts/fuzzy_date_field', form: form,
                     prefix: :start_date, fdate: @participant.f_start_date %>
        </p>

        <% unless @participant.f_end_date.nil? %>
            <p>
              <%= form.label :end_date, t(:End_date) %>
              <%= render 'layouts/fuzzy_date_field', form: form,
                    prefix: :end_date, fdate: @participant.f_end_date %>
            </p>
        <% end %>

        <%= render 'associated_activities', associated_activities: @participant.activities %>

        <p>
          <%= form.submit t(:Save) %>
        </p>
    <% end %>
<% end %>