<%
  # Locals: sub1, value, master_checked, params
%>

<%=
  check_box_tag "elements[#{sub1}][]",
                  value,
                  params[:elements].try(:[], sub1.to_sym).try(:include?, value),
                  disabled: !master_checked
%>