<% participants = activity.participants_for_my_stage %>
<% participants.each do |participant| %>
    <%= link_to participant.person.full_name, participant.person %><%= ', ' unless participant==participants.last %>
<% end %>